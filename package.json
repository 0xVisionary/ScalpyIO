{
  "name": "scalpyio",
  "version": "1.0.0",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "cd elizatrader && npm ci --only=production && node server.js",
    "heroku-postbuild": "cd elizatrader && npm ci"
  }
}